# 앱인토스 AI 에이전트

토스 미니앱 아이디어 생성부터 출시까지 지원하는 AI 에이전트 시스템

## 🎯 기능

### 10가지 AI 스킬
1. **아이디어 생성기** - 카테고리/키워드/문제 기반 아이디어 생성
2. **기획서 작성기** - 개발 가능한 상세 기획서 자동 생성
3. **시장 분석기** - 경쟁사 분석, SWOT, 시장 규모 산출
4. **기술 스택 추천** - 프로젝트에 맞는 최적 기술 스택 제안
5. **수익화 전략** - 7가지 수익 모델 중 최적 전략 제시
6. **UX 디자이너** - 토스 디자인 시스템 기반 UI/UX 가이드
7. **코드 생성기** - React 보일러플레이트 자동 생성
8. **토스 SDK 헬퍼** - 토스 SDK 통합 가이드 제공
9. **피드백 분석기** - 사용자 피드백 감성 분석
10. **출시 플래너** - 마케팅 & 런칭 계획 수립

### 4개 탭 구조
- **아이디어**: 대화형/카테고리별/랜덤 생성
- **분석**: 시장 분석 & 수익화 전략
- **개발**: 기술 스택, UI/UX, 코드 생성
- **출시**: 마케팅 계획 & 출시 일정

## 📦 파일 구조

```
receipt-static/
├── demo-agent.html              # 즉시 실행 가능한 데모 (★ 이것부터 실행)
├── AppsInTossAgentWithSkills.jsx  # 완전한 에이전트 컴포넌트
├── apps-in-toss-agent.html      # 스킬 시스템 통합 HTML
└── skills/                       # AI 스킬 시스템
    ├── index.js                 # 모듈 내보내기
    ├── BaseSkill.js             # 기본 스킬 클래스
    ├── SkillManager.js          # 스킬 오케스트레이션
    ├── PromptEngine.js          # AI 프롬프트 생성
    ├── useSkills.js             # React Hooks
    ├── ideaGenerator.js         # 아이디어 생성
    ├── specWriter.js            # 기획서 작성
    ├── marketAnalyzer.js        # 시장 분석
    ├── techStackAdvisor.js      # 기술 스택 추천
    ├── monetizationPlanner.js   # 수익화 전략
    ├── uxDesigner.js            # UX 디자인
    ├── codeGenerator.js         # 코드 생성
    ├── tossSdkHelper.js         # 토스 SDK 헬퍼
    ├── feedbackAnalyzer.js      # 피드백 분석
    └── launchPlanner.js         # 출시 계획
```

## 🚀 빠른 시작

### 옵션 1: 데모 실행 (★ 추천)

1. `demo-agent.html` 파일을 브라우저에서 열기
2. 바로 사용 가능!

### 옵션 2: 개발 환경에서 실행

#### 1. Vite + React 프로젝트 생성

```bash
# 새 프로젝트 생성
npm create vite@latest toss-miniapp-agent -- --template react

# 프로젝트 디렉토리로 이동
cd toss-miniapp-agent

# 의존성 설치
npm install
npm install lucide-react
```

#### 2. 파일 복사

```bash
# skills 폴더 전체 복사
cp -r ../receipt-static/skills ./src/

# 에이전트 컴포넌트 복사
cp ../receipt-static/AppsInTossAgentWithSkills.jsx ./src/
```

#### 3. App.jsx 수정

```jsx
import AppsInTossAgent from './AppsInTossAgentWithSkills'

function App() {
  return <AppsInTossAgent />
}

export default App
```

#### 4. 실행

```bash
npm run dev
```

브라우저에서 http://localhost:5173 접속

### 옵션 3: Next.js 프로젝트

```bash
# Next.js 프로젝트 생성
npx create-next-app@latest toss-miniapp-agent
cd toss-miniapp-agent

# 의존성 설치
npm install lucide-react

# skills 폴더 복사
cp -r ../receipt-static/skills ./src/

# 컴포넌트 복사
cp ../receipt-static/AppsInTossAgentWithSkills.jsx ./src/app/
```

`app/page.js` 수정:

```jsx
import AppsInTossAgent from './AppsInTossAgentWithSkills'

export default function Home() {
  return <AppsInTossAgent />
}
```

## 💡 사용 예시

### 1. 아이디어 생성
```jsx
import { useIdeaGenerator } from './skills/useSkills'

function MyComponent() {
  const { generateByCategory, ideas } = useIdeaGenerator()

  const handleGenerate = async () => {
    await generateByCategory('finance')
    console.log(ideas) // 생성된 아이디어 배열
  }

  return <button onClick={handleGenerate}>금융 아이디어 생성</button>
}
```

### 2. 기획서 작성
```jsx
import { useSpecWriter } from './skills/useSkills'

function MyComponent() {
  const { generateSpec, spec, downloadMarkdown } = useSpecWriter()

  const handleGenerate = async () => {
    const idea = { title: '매일 100원 챌린지', ... }
    await generateSpec(idea)
    downloadMarkdown() // 마크다운 파일 다운로드
  }

  return <button onClick={handleGenerate}>기획서 생성</button>
}
```

### 3. 완전 자동화 워크플로우
```jsx
import { useCompleteWorkflow } from './skills/useSkills'

function MyComponent() {
  const { runCompleteWorkflow, results } = useCompleteWorkflow()

  const handleComplete = async () => {
    await runCompleteWorkflow({
      category: 'finance',
      keywords: ['저축', '챌린지']
    })

    // 모든 결과 한 번에
    console.log(results.idea)        // 생성된 아이디어
    console.log(results.spec)        // 상세 기획서
    console.log(results.marketAnalysis) // 시장 분석
    console.log(results.design)      // UI/UX 가이드
    console.log(results.code)        // 생성된 코드
    console.log(results.launchPlan)  // 출시 계획
  }

  return <button onClick={handleComplete}>전체 프로세스 실행</button>
}
```

## 🎨 커스터마이징

### 새로운 스킬 추가

1. `skills/` 폴더에 새 파일 생성 (예: `myCustomSkill.js`)
2. `BaseSkill`을 상속받아 구현
3. `skills/index.js`에 export 추가
4. `SkillManager.js`에 스킬 등록

```javascript
// skills/myCustomSkill.js
import BaseSkill from './BaseSkill';

class MyCustomSkill extends BaseSkill {
  constructor() {
    super('myCustomSkill', {
      name: '커스텀 스킬',
      description: '내가 만든 스킬',
      inputSchema: { /* 스키마 */ }
    });
  }

  async execute(input) {
    // 로직 구현
    return { success: true, data: result };
  }
}

export default new MyCustomSkill();
```

## 📝 참고사항

- **데모 버전**: AI 스킬은 시뮬레이션되며 실제 API 호출 없음
- **완전 버전**: 실제 AI API (GPT, Claude 등)와 연동 필요
- **토스 SDK**: 실제 토스 미니앱에서 동작하려면 토스 SDK 설정 필요
- **디자인 시스템**: 토스의 공식 디자인 가이드라인 기반

## 🔗 관련 링크

- [토스 플랫폼 문서](https://toss.im/platform)
- [React 공식 문서](https://react.dev)
- [Tailwind CSS](https://tailwindcss.com)

## 📄 라이선스

MIT License
